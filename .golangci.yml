# SPDX-License-Identifier: Apache-2.0

version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - goimports
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - misspell
    - revive
    - gocritic

settings:
  goimports:
    local-prefixes:
      - github.com/hbraswelrh/gemara-user-journey
  revive:
    rules:
      - name: blank-imports
      - name: exported
      - name: unexported-return
      - name: var-naming
      - name: error-return
      - name: error-naming
      - name: increment-decrement
  gocritic:
    enabled-checks:
      - appendAssign
      - dupArg
      - dupCase
      - dupSubExpr
      - flagName
      - sloppyLen
  misspell:
    locale: US

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
